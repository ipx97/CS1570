/*
Programmer: Ian Piskulic      Date: 4/26/2015
File: hw10.h                 Section: B
Purpose: DO THIS STILL
*/

#include <iostream>
#include "res.h"
#include "room.h"
#include "csi_agent.h"

using namespace std;

#ifndef HW10_H
#define HW10_H

//Structs
struct finger_print{
  char print [SIZE2D][SIZE2D];
  int loops,arches,whirls;
  finger_print();
};

struct forensic_data{
  int hairColor;
  char dna[DNA_SIZE];
  finger_print fingerPrint;
  forensic_data operator = (const forensic_data rhs);
};

//Prototypes
void greeting();
void encounters(Resident res[], CrimeRoom & room);
short resCount(const Resident res[]);
void investigation(CSI_Agent & agent, Resident res[],
  const CrimeRoom & room, Judicial & judge));
void ending(const Resident res[]);
void signoff();


//Constants
const int DNA_SIZE = 128;
const int SIZE2D = 8;
const int RES_NUM = 25;
const int VISITS_MAX = 10;
